version: "3"

services:
  server-service:
    build: ./server
    volumes:
      - ./server:/usr/src/app
    ports:
      - 5001:80

  client-service:
    build: ./client
    volumes:
      - ./client:/usr/src/app
    ports:
      - 5000:80
    depends_on:
      - server-service
